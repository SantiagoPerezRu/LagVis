<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@android:color/transparent"/>  <!-- Fondo transparente -->
    <stroke android:width="2dp" android:color="#000000"/>  <!-- Borde negro -->
    <corners android:radius="4dp"/>  <!-- Bordes ligeramente redondeados -->
    <padding android:left="8dp" android:top="4dp" android:right="8dp" android:bottom="4dp"/>
</shape>
